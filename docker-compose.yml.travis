version: '2'

services:
  mysql:
    image: mysql:5.7
    restart: always
    command: mysqld --skip-name-resolve --max_allowed_packet=64M
    env_file:
      - ./env/mysql.env
    expose:
     - "3306"

  hit.lib.unb.ca:
    image: hit.lib.unb.ca:latest
    restart: always
    container_name: hit.lib.unb.ca
    expose:
      - "80"
    ports:
      - "80:80"
    env_file:
      - ./env/drupal.env
